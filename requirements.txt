# Social Sentiment Pipeline Dependencies
# =======================================

# API Service - Flask
flask>=3.0.0
gunicorn>=21.0.0

# API Service - FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# Database
psycopg2-binary>=2.9.9

# HTTP Client
aiohttp>=3.9.0
requests>=2.31.0

# Environment
python-dotenv>=1.0.0

# Telegram Client API (Telethon)
telethon>=1.34.0

# Twitter/X Scraping (twikit - no API key required)
# Note: We use Syndication API scraper, twikit is optional fallback
twikit>=2.3.0
httpx[socks]>=0.27.0
python-socks[asyncio]>=2.5.0

# Twitter Scraping (optional - ntscraper via Nitter)
# ntscraper>=0.3.0  # Uncomment to enable Twitter scraping

# Optional: LLM integration for fallback classification
# Uncomment if using Gemini API for LLM-based sentiment classification
# google-generativeai>=0.3.0
